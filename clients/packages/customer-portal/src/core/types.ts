import type { schemas } from '@rapidly-tech/client'

/** Schema type representing a customer in the customer portal. */
export type CustomerPortalCustomer = schemas['CustomerPortalCustomer']
/** Schema type for updating a customer profile in the customer portal. */
export type CustomerPortalCustomerUpdate =
  schemas['CustomerPortalCustomerUpdate']

export type { schemas }

# Rapidly analytics — aggregated user event activity per hour for active-customer counting
SCHEMA >
    `organization_id` UUID,
    `hour` DateTime,
    `customer_id` Nullable(UUID),
    `external_customer_id` Nullable(String)

ENGINE MergeTree
ENGINE_SORTING_KEY organization_id, hour

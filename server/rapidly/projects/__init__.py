"""Project management domain: projects, states, labels, estimates, and work items.

A second product line inside Rapidly that lives alongside the existing
file-sharing modules.  Every submodule follows the standard layout
(``api.py``, ``actions.py``, ``queries.py``, ``types.py``,
``permissions.py``, ``ordering.py``) so the conventions remain uniform
across the codebase.
"""

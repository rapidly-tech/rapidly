"""Page module: project-scoped wiki/doc CRUD.

Yjs collaborative editing is intentionally out of scope here — the
sync server is a follow-up phase.  The model carries the binary slot
today so the schema is ready when sync lands.
"""

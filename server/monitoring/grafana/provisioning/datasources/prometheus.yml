# Rapidly — Grafana data-source provisioning
# Auto-loaded on container start; connects Grafana to the local Prometheus.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
